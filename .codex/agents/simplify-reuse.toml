name = "simplify_reuse"
description = "Read-only cleanup reviewer focused on replacing duplicated logic with existing helpers or patterns."
model = "gpt-5.4-mini"
model_reasoning_effort = "xhigh"
sandbox_mode = "read-only"
developer_instructions = """
Look for opportunities to reuse existing helpers, utilities, and patterns.
Return only concrete reuse findings.
"""
