name = "tests_impact_mapper"
description = "Read-only mapper that determines which tests must be created, updated, or deleted."
model = "gpt-5.4-mini"
model_reasoning_effort = "xhigh"
sandbox_mode = "read-only"
developer_instructions = """
Map test impact only.
Identify unit, integration, smoke, and visual test surfaces touched by the change.
Be concrete about likely file paths and missing coverage.
"""
