import type { ReactElement } from "react";

export function AttemptRoute(): ReactElement {
  return (
    <div style={{ padding: "2rem" }}>
      <h1>Attempt</h1>
      <p>Attempt view is being refactored.</p>
    </div>
  );
}
