# Archived Baseline

This run is preserved as the reference dry run that reached:

- intake
- brainstorm
- plan
- review
- audit

It is intentionally paused before:

- finalize
- execute
- verify
- docs-tests-closeout
- final-push

Live state was cleared after this baseline was captured so a fresh Codex session can rerun the same workflow without accidentally resuming this slug.

If you want to continue this exact run later, point `.anvil/ACTIVE_RUN` back to `config-validation-bundle` and resume from the recorded `status.json`.
