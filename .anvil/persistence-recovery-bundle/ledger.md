# Debate Ledger

## Settled

- **L1.** `#278` is already shipped and stays as context plus dependency surface, not fresh implementation scope.
- **L2.** Dirty-workspace protection belongs in the workspace removal choke point so every cleanup path benefits, not just the post-success git path.
- **L3.** Recovery should reuse the existing attempt row and `thread/resume` path for viable sessions so the resumed run is truthful to operators.
- **L4.** Ambiguous orphan cases should be reported, not silently retried or silently deleted.

## Contested

*(none)*

## Open

*(none)*

## Score History

- Round 1: 8.4/10, GO, settled 4, contested 0, open 0
